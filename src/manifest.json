{
  "name": "MDX Path → Docs URL",
  "description": "Reads a file path from clipboard and opens the corresponding help article.",
  "version": "1.4",
  "manifest_version": 3,
  "permissions": ["tabs"],
  "background": {
    "service_worker": "background.js"
  },
  "action": {
    "default_popup": "popup.html"
  },
  "commands": {
    "open-from-clipboard": {
      "suggested_key": {
        "default": "Ctrl+Shift+U"
      },
      "description": "Helps writers quickly open prod or stage articles from copied file path. Works for relative file path copied from VSCode tab of open file or for a root relative link added inline in an article."
    }
  }
}